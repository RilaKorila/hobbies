screenWidth = 400;
screenHeight = 400;

function setup() {
    createCanvas(screenWidth, screenHeight);
}

function draw() {
    background(220);
}

function loop(){
    ctx.clearRect(0, 0, screenWidth, screenHeight);


}
